{
  "name": "design-system",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.2.3"
  },
  "packageManager": "npm@10.7.0",
  "dependencies": {
    "@storybook/addon-a11y": "^8.4.4"
  }
}
